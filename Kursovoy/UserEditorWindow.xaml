<Window x:Class="Kursovoy.UserEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование пользователя" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="0" Margin="100,0,0,5" Height="25"/>

        <TextBlock Text="Пароль:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="PasswordTextBox" Grid.Row="1" Margin="100,0,0,5" Height="25"/>

        <TextBlock Text="ID Роли:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="RoleIDTextBox" Grid.Row="2" Margin="100,0,0,5" Height="25"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="100" Margin="5" Click="Save_Click"/>
            <Button Content="Отмена" Width="100" Margin="5" Click="Cancel_Click"/>
            <Button Content="Роли" Width="100" Margin="5" Click="Role_Click"/>

        </StackPanel>
    </Grid>
</Window>
